package pl.competencyproject.model.mechanicsOfQuestion.interfaces;

public interface ITeacher {
    /*
    To powina zawierać klasa Teacher
    classa Word musi posiadać licznik odpytań domyślnie jest to 3 jak słowo pojawi się dla użytkownika w pytniu licznik spada o 1
    jak licznik osiągnie 0 to dane słowo wypada z  currentMapQuestion


    Map<Word,LinkedList<String> mapAllWords;   Word content( Id from DB and String from DB
    SortedMap<Word,LinkedList<String> = new HashMap<Word,Map<Word,LinkedList<String> currentMapQuestion; // zawiera listę pytań dla poszczególnych sesji
    //Sortujemy Mapę po wordach to jest na początku mapy znajdują się wordy z najwyższym licznikiem użyć czyli 3;

    //potrzebujemy licznik sesji domyślnie będzie miał wartość 1;
    //potrzebujemy również policzyć ile sesji bez powtórzeń możemy zrobić tj. potrzebujemy zmiennej int maxSession
    Wyliczymy ją za pomocą dwumianu Newtona tj.
    n = mapAllWords.size();
    k = 10
    maxSession = (factorial(n)/factorial(k)*(factorial(n-k))

    Po każdych 10 pytanich pytamy użytkownika czy chce dalej ćwiczyć słowa z tego poziomu ( Jeśli tak licznikSession++ Jeśli nie


     Potrzebujemy zmiennych na zliczanie prawidłowych i błędnych odpowiedzi
     powrót do menu głównego oraz
    wynulowanie map by pozwolić by GarbageCollector sobie kiedyś zwolnił tą pamięć

    Jak licznik sesji  zrówna się z maxSession wtedy zakończyłeś naukę tego poziomu się wyświetli





*/
}
